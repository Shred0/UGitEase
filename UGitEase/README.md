# UGitEase

to create a new View:
	- right click on View folder, add a C# item and name it with the new view's name
	- run "dotnet new avalonia.usercontrol -o Views -n ToDoListView  --namespace UGitEase.Views" command in rootfolder
